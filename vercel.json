{
    "version": 2,
    "name": "asssignment-10-server",
    "builds": [
       { "src": "server.js", "use": "@vercel/node" }
    ],
    "routes": [
       { "src": "/(.*)", "dest": "/server.js" }
    ],
    "functions": {
        "myFunction": {
          "runtime": "nodejs16.x",
          "memory": 512,
          "timeout": 20
        }
      }
 }